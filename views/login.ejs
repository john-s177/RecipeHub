<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #FFE0B5;
            font-family: Arial, sans-serif;
        }
        .container {
            max-width: 400px;
            margin-top: 100px;
            background-color: #ffcb94;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
            color: white;
        }
        .btn-peach {
            background-color: #FFAB76;
            color: black;
        }
        .btn-peach:hover {
            background-color: #FF8F56;
        }
        .footer {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 1rem;
            background-color: #FFE0B5;
            margin-top: 20px;
        }
        .footer span {
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Login</h1>
        <% if (locals.message) { %>
            <h5><%= message %></h5>
        <% } %>
        <form action="/login" method="POST">
            <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input type="email" class="form-control" id="email" name="email" size="30" autocomplete="off" required>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input type="password" class="form-control" id="password" name="password" size="30" autocomplete="off" required>
            </div>
            <p>Don't Have an Account? <a href="/register">Register</a> Now</p>
            <button type="submit" class="btn btn-peach w-100">Submit</button>
        </form>
    </div>
    <footer class="footer">
        <span id="footerText"></span>
    </footer>

    <script>
        const getYear = () => new Date().getFullYear();
        document.getElementById('footerText').textContent = `RecipeHub © ${getYear()}, Developed By John Sameh`;
    </script>
</body>
</html>
